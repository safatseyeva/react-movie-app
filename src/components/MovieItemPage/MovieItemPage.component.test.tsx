import * as React from 'react';
import { render, fireEvent, waitForElement } from '@testing-library/react';

import MovieItemPage, { MovieItemPageProps } from './MovieItemPage.component';

describe('<MovieItemPage />', () => {
  test.only('should set state movie and moviesToShow ', async () => {
    // ???
  });
});